export enum LLM_PROVIDER_TYPES {
  OPENAI = 'openai',
  LMSTUDIO = 'lmstudio',
  MOCK = 'mock'
}

export enum REPOSITORY_TYPES {
  IN_MEMORY = 'inmemory',
  MONGODB = 'mongodb'
}

export const SERVICES = {
    LLM: 'LLMApplicationService',
    USER_REPOSITORY: 'UserRepository'
}

